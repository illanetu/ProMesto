# 🚀 Инструкция по деплою на Vercel

## Шаг 1: Подготовка базы данных на Neon

### 1.1. Создание таблицы через Neon Dashboard

1. **Откройте Neon Dashboard**
   - Перейдите на [console.neon.tech](https://console.neon.tech)
   - Войдите в свой аккаунт

2. **Найдите SQL Editor**
   - Выберите ваш проект из списка проектов
   - В левом боковом меню найдите пункт **"SQL Editor"**
   - Или используйте вкладку "SQL Editor" в верхней панели

3. **Выполните SQL запрос**
   - В редакторе SQL вы увидите пример запроса (можно его удалить)
   - Очистите редактор (Ctrl+A → Delete) или нажмите "New Query"
   - Скопируйте следующий SQL код из файла `prisma/migrations/init.sql`:
   
   ```sql
   CREATE TABLE IF NOT EXISTS "notes" (
       "id" TEXT NOT NULL,
       "title" TEXT NOT NULL,
       "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
       CONSTRAINT "notes_pkey" PRIMARY KEY ("id")
   );
   ```
   
   - Вставьте код в SQL Editor
   - Нажмите кнопку **"Run"** (или используйте горячие клавиши `Ctrl+Enter` / `Cmd+Enter`)

4. **Проверка**
   - Должно появиться сообщение об успешном выполнении
   - Таблица `notes` создана в базе данных

### 1.2. Получение строки подключения

1. В Neon Dashboard откройте ваш проект
2. Перейдите в раздел **"Connection Details"** или **"Connect"**
3. Скопируйте **Connection String** (строка подключения)
4. Сохраните её — понадобится для Vercel

## Шаг 2: Деплой на Vercel

### 2.1. Через веб-интерфейс Vercel

1. **Подготовка репозитория**
   ```powershell
   git init
   git add .
   git commit -m "Начальный коммит проекта"
   git remote add origin <ваш-репозиторий-на-github>
   git push -u origin main
   ```

2. **Импорт проекта на Vercel**
   - Откройте [vercel.com](https://vercel.com)
   - Нажмите **"Add New Project"**
   - Импортируйте ваш Git репозиторий (GitHub/GitLab/Bitbucket)
   - Vercel автоматически определит Next.js проект

3. **Настройка переменных окружения**
   - В разделе **"Environment Variables"** добавьте:
     - **Name**: `DATABASE_URL`
     - **Value**: ваша строка подключения из Neon (скопированная на шаге 1.2)
   - Убедитесь, что переменная доступна для всех окружений (Production, Preview, Development)

4. **Деплой**
   - Нажмите **"Deploy"**
   - Дождитесь завершения сборки

### 2.2. Через Vercel CLI

```powershell
# Установка Vercel CLI (если ещё не установлен)
npm i -g vercel

# Логин в Vercel
vercel login

# Деплой проекта
vercel

# Следуйте инструкциям в терминале
# Когда спросит про переменные окружения, добавьте DATABASE_URL
```

После деплоя добавьте переменную `DATABASE_URL` в настройках проекта на Vercel (Settings → Environment Variables).

## Шаг 3: Проверка работы

1. Откройте URL вашего проекта на Vercel
2. Должна отобразиться главная страница
3. Если таблица пустая, вы увидите сообщение "Нет заметок в базе данных"
4. Для заполнения тестовыми данными выполните локально:
   ```powershell
   vercel env pull .env.local
   npm run db:seed
   ```

## ✅ Готово!

Ваш проект развернут на Vercel и подключен к NeonDB. Главная страница читает данные из PostgreSQL и отображает их.

## 🔧 Решение проблем

### Ошибка подключения к БД на Vercel
- Проверьте, что переменная `DATABASE_URL` добавлена в настройках Vercel
- Убедитесь, что строка подключения правильная (скопирована из Neon Dashboard)
- Проверьте, что таблица `notes` создана в базе данных

### Таблица не существует
- Выполните SQL из `prisma/migrations/init.sql` в Neon Dashboard → SQL Editor
- Или используйте локально: `npm run db:create-table` (после `vercel env pull .env.local`)

### Нет данных на странице
- Это нормально, если таблица пустая
- Заполните данные через seed: `npm run db:seed` (локально с переменными из Vercel)
