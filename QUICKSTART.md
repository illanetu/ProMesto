# ⚡ Быстрый старт

## Шаг 1: Установка зависимостей

```powershell
npm install
```

## Шаг 2: Создание .env файла

```powershell
New-Item -Path .env -ItemType File -Force
```

Добавьте в `.env`:

```
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
```

**Получите строку подключения на [neon.tech](https://neon.tech)**

## Шаг 3: Настройка базы данных

```powershell
# Генерация Prisma Client
npm run db:generate

# Создание миграции (введите имя: init)
npm run db:migrate

# Заполнение тестовыми данными
npm run db:seed
```

## Шаг 4: Запуск

```powershell
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 🚀 Деплой на Vercel

1. Закоммитьте код в Git
2. Импортируйте репозиторий на [vercel.com](https://vercel.com)
3. Добавьте переменную окружения `DATABASE_URL` в настройках проекта
4. Нажмите Deploy

**Готово!** 🎉
