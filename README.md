# ProMesto

Минимальный рабочий проект на Next.js (App Router) + Prisma + NeonDB (PostgreSQL), готовый к деплою на Vercel.

## 🚀 Технологии

- **Next.js 14** (App Router, TypeScript)
- **Prisma** (ORM)
- **NeonDB** (PostgreSQL)
- **Vercel** (деплой)

## 📋 Требования

- Node.js 18+ 
- npm или yarn
- Аккаунт на [Neon.tech](https://neon.tech) для базы данных

## 🛠️ Установка и запуск

### 1. Установка зависимостей

```powershell
npm install
```

### 2. Настройка базы данных

1. Создайте проект на [Neon.tech](https://neon.tech)
2. Скопируйте строку подключения (Connection String)
3. Создайте файл `.env` в корне проекта:

```powershell
# Создайте .env файл
New-Item -Path .env -ItemType File -Force
```

4. Откройте `.env` и вставьте строку подключения:

```
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
```

**Важно:** Замените `user:password@host/database` на ваши реальные данные из Neon.tech

### 3. Настройка Prisma

```powershell
# Генерация Prisma Client
npm run db:generate

# Создание таблиц в базе данных (выберите один из вариантов)
```

**Вариант 1: Автоматическое создание через скрипт (рекомендуется)**
```powershell
npm run db:create-table
```

**Вариант 2: Ручное создание через SQL**
1. Откройте Neon Dashboard → ваш проект → SQL Editor
2. Выполните SQL из файла `prisma/migrations/init.sql`

**Вариант 3: Prisma db push (может зависать на Neon)**
```powershell
npm run db:push
```

**Вариант 4: Prisma Migrate (не работает на Neon из-за shadow database)**
```powershell
npm run db:migrate
```

После создания таблиц:
```powershell
# Заполнение базы тестовыми данными
npm run db:seed
```

### 4. Запуск проекта

```powershell
# Режим разработки
npm run dev

# Сборка для продакшена
npm run build

# Запуск продакшен версии
npm start
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📦 Структура проекта

```
ProMesto/
├── app/
│   ├── layout.tsx          # Корневой layout
│   ├── page.tsx            # Главная страница (читает данные из БД)
│   └── globals.css         # Глобальные стили
├── lib/
│   └── prisma.ts           # Singleton для Prisma Client
├── prisma/
│   ├── schema.prisma       # Схема базы данных
│   └── seed.ts             # Seed скрипт для заполнения БД
├── .env                    # Переменные окружения (не в git)
├── .env.example            # Пример переменных окружения
├── next.config.js          # Конфигурация Next.js
├── package.json            # Зависимости и скрипты
├── tsconfig.json           # Конфигурация TypeScript
└── vercel.json             # Конфигурация для Vercel
```

## 🗄️ Модель данных

### Note

- `id` (String, UUID) - уникальный идентификатор
- `title` (String) - заголовок заметки
- `createdAt` (DateTime) - дата создания

## 🚢 Деплой на Vercel

### 1. Подготовка

1. Убедитесь, что проект закоммичен в Git
2. Создайте репозиторий на GitHub/GitLab/Bitbucket

### 2. Деплой через Vercel CLI

```powershell
# Установка Vercel CLI (если еще не установлен)
npm i -g vercel

# Логин в Vercel
vercel login

# Деплой
vercel
```

### 3. Деплой через веб-интерфейс

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите "Add New Project"
3. Импортируйте ваш Git репозиторий
4. В настройках проекта добавьте переменную окружения:
   - **Name**: `DATABASE_URL`
   - **Value**: ваша строка подключения к NeonDB
5. Нажмите "Deploy"

### 4. Настройка переменных окружения на Vercel

После деплоя:

1. Перейдите в Settings → Environment Variables
2. Добавьте `DATABASE_URL` со значением вашей строки подключения NeonDB
3. Перезапустите деплоймент

### 5. Создание таблицы в базе данных

После первого деплоя необходимо создать таблицу в базе данных. Выберите один из вариантов:

**Вариант 1: Через Neon Dashboard (рекомендуется)**

Пошаговая инструкция:

1. **Войдите в Neon Dashboard**
   - Откройте [console.neon.tech](https://console.neon.tech)
   - Войдите в свой аккаунт

2. **Откройте SQL Editor**
   - Выберите ваш проект из списка
   - В левом меню найдите раздел **"SQL Editor"** (или нажмите на вкладку "SQL Editor" вверху)
   - Откроется редактор SQL с пустым полем для ввода запросов

3. **Скопируйте и выполните SQL запрос**
   - Откройте файл `prisma/migrations/init.sql` в вашем проекте
   - Скопируйте весь SQL код (без комментариев, если хотите):
   ```sql
   CREATE TABLE IF NOT EXISTS "notes" (
       "id" TEXT NOT NULL,
       "title" TEXT NOT NULL,
       "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
       CONSTRAINT "notes_pkey" PRIMARY KEY ("id")
   );
   ```
   - Вставьте этот код в SQL Editor на Neon Dashboard
   - Нажмите кнопку **"Run"** (или `Ctrl+Enter` / `Cmd+Enter`)

4. **Проверка результата**
   - Если всё успешно, вы увидите сообщение типа "Success" или "Query executed successfully"
   - Таблица `notes` создана в вашей базе данных
   - Теперь можно заполнить её данными через seed или использовать в приложении

**Альтернативно:** Если хотите проверить, что таблица создана, выполните в SQL Editor:
```sql
SELECT * FROM notes;
```
(Должен вернуть пустой результат, если данных ещё нет)

**Вариант 2: Через Vercel CLI (локально)**
```powershell
# Получить переменные окружения
vercel env pull .env.local

# Создать таблицу
npm run db:create-table

# Заполнить тестовыми данными (опционально)
npm run db:seed
```

## 📝 Полезные команды

```powershell
# Разработка
npm run dev

# Сборка
npm run build

# Запуск продакшен версии
npm start

# Prisma команды
npm run db:generate    # Генерация Prisma Client
npm run db:migrate     # Создание миграции
npm run db:seed        # Заполнение БД тестовыми данными
npm run db:studio      # Открыть Prisma Studio (GUI для БД)
```

## ✅ Проверка работы

После деплоя на Vercel:

1. Откройте URL вашего проекта
2. Должна отобразиться главная страница с заметками из PostgreSQL
3. Если заметок нет, выполните `npm run db:seed` локально или через Vercel CLI

## 🔧 Решение проблем

### Ошибка подключения к БД

- Проверьте правильность `DATABASE_URL` в `.env`
- Убедитесь, что NeonDB проект активен
- Проверьте, что строка подключения содержит `?sslmode=require`

### Ошибка при миграции

- Убедитесь, что Prisma Client сгенерирован: `npm run db:generate`
- Проверьте права доступа к базе данных

### Ошибка на Vercel

- Проверьте, что переменная `DATABASE_URL` добавлена в настройках Vercel
- Убедитесь, что в `package.json` есть скрипт `build` с `prisma generate`

## 📄 Лицензия

MIT
