# 🚀 Быстрый старт - Команды PowerShell

## 1. Создание проекта (уже выполнено)

Проект уже создан. Если нужно создать заново:

```powershell
npx create-next-app@latest . --typescript --app --no-tailwind --eslint
```

## 2. Установка зависимостей

```powershell
npm install
```

## 3. Настройка .env файла

Создайте файл `.env` в корне проекта:

```powershell
# Создайте файл .env
New-Item -Path .env -ItemType File -Force
```

Добавьте в `.env` следующее содержимое (замените на вашу строку подключения NeonDB):

```
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
```

**Как получить строку подключения:**
1. Зарегистрируйтесь на [neon.tech](https://neon.tech)
2. Создайте новый проект
3. Скопируйте Connection String из настроек проекта
4. Вставьте в `.env` файл

## 4. Настройка Prisma

```powershell
# Генерация Prisma Client
npm run db:generate

# Создание и применение миграции
npm run db:migrate
# При запросе имени миграции введите: init

# Заполнение базы тестовыми данными
npm run db:seed
```

## 5. Запуск проекта

```powershell
# Режим разработки
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 6. Деплой на Vercel

### Вариант 1: Через веб-интерфейс

1. Закоммитьте изменения в Git:
```powershell
git init
git add .
git commit -m "Начальный коммит проекта"
```

2. Создайте репозиторий на GitHub и запушьте:
```powershell
git remote add origin <ваш-репозиторий>
git push -u origin main
```

3. На [vercel.com](https://vercel.com):
   - Импортируйте репозиторий
   - Добавьте переменную окружения `DATABASE_URL`
   - Нажмите Deploy

### Вариант 2: Через Vercel CLI

```powershell
# Установка Vercel CLI
npm i -g vercel

# Логин
vercel login

# Деплой
vercel

# Следуйте инструкциям в терминале
```

После деплоя добавьте переменную окружения `DATABASE_URL` в настройках проекта на Vercel.

## 7. Применение миграций на Vercel

После первого деплоя нужно применить миграции:

```powershell
# Получить переменные окружения
vercel env pull .env.local

# Применить миграции
npx prisma migrate deploy
```

Или добавьте в Build Command на Vercel:
```
prisma generate && prisma migrate deploy && next build
```

## ✅ Проверка

После деплоя откройте URL вашего проекта на Vercel. Должна отобразиться страница с заметками из PostgreSQL.

Если заметок нет, выполните seed локально или через Vercel CLI.
