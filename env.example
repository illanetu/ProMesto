# Скопируйте в .env и заполните значения.
# База данных (PostgreSQL, NeonDB и др.)
# Для Neon: ОБЯЗАТЕЛЬНО pooled URL (host с суффиксом -pooler) — иначе ConnectionReset при входе через Google.
# connection_limit: для локальной разработки лучше 2–3 (иначе «Timed out fetching connection» при auth + dashboard).
# Для serverless (Vercel) можно connection_limit=1.
# DATABASE_URL — pooled (Prisma Client), DIRECT_URL — прямой (миграции, без -pooler).
# Для обычного PostgreSQL можно задать оба одинаковыми.
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech:5432/dbname?sslmode=require&connection_limit=3"
DIRECT_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech:5432/dbname?sslmode=require"

# Auth.js: секрет для подписи сессий (обязательно в production)
AUTH_SECRET="сгенерируйте-длинную-случайную-строку"

# Google OAuth (Console: https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID="ваш-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="ваш-google-client-secret"
