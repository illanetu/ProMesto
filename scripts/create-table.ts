/**
 * Раньше создавал только таблицу notes.
 * Теперь схема в миграциях — применяйте их:
 *
 *   npx prisma migrate deploy
 *
 * (на Neon не используйте "prisma migrate dev" из-за shadow database)
 */
console.log('Используйте: npx prisma migrate deploy')
process.exit(0)
